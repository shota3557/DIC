<h1>投稿詳細画面</h1>
<div>
  <strong>Title:</strong>
  <%= @blog.title %>
</div>
<div>
  <strong>Content:</strong>
  <%= @blog.content %>
</div>

<div>
  <%= link_to '編集', blog_path(@blog.id) %>
</div >
<div>
  <%= link_to '削除', blog_path(@blog.id), method: :delete, data: {confirm: '本当に削除しますか?'} %>
</div>