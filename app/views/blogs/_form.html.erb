<%= form_with(modle: blog, local: true) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>
  <%= form.label :content %>
  <%= form.text_area :content %>
  <%= form.submit '登録' %>
<% end %>

<%= link_to 'Back', blogs_path %>